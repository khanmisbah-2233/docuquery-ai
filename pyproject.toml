[project]
name = "docuquery-ai"
version = "1.0.0"
description = "Intermediate PDF RAG system using Pinecone vector database."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-p no:cacheprovider"
